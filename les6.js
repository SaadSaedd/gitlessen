function sponge(stringa){
    let resultaat =""
    
    return stringa.toLowerCase();
    return stringb.toUppercase();

}
let gemoktestring = sponge("Ik heb een beker water");
console.log(">>"+gemoktestring);

  function ShakeFunctie(woordDatGeschudtMoetWorden){
    
        const characters = woordDatGeschudtMoetWorden.split('');
        
          for (let i = characters.length - 1; i > 0; i--) {
            const j = Math.floor(Math.random() * (i + 1));
            
            stringa.toLowerCase();
            stringb.toUppercase();
          }
          const geschudWoord = characters.join('');
          	  	
        }
      